apiVersion: apps/v1
kind: Deployment
metadata:
  name: attendance-monitoring-deployment
  labels:
    app.kubernetes.io/name: attendance-monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: attendance-monitoring
  template:
    metadata:
      labels:
        app.kubernetes.io/name: attendance-monitoring
    spec:
      volumes:
        - name: attendance-monitoring-pv-storage
          persistentVolumeClaim:
            claimName: attendance-monitoring-pvc
      containers:
      - name: redis
        image: redis:7
        ports:
        - containerPort: 6379
      - name: web
        image: ghcr.io/navaneeth-dev/attendance-monitoring-web:main
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
          - name: DJANGO_DEBUG
            value: "true"
          - name: SECRET_KEY
            value: '$#GFHhui45khfdjhgblkshsSfHllldfHYUN#EjgksEHm,GBFJK'

        #  volumeMounts:
        #  - mountPath: "/pb/pb_data"
        #    name: attendance-monitoring-pv-storage
        #      - name: cron
        #        image: ghcr.io/navaneeth-dev/attendance-monitoring-cron:main
        #        imagePullPolicy: Always
        #        env:
        #        - name: C_FORCE_ROOT
        #          value: "true"
        #        - name: REDIS_URL
        #          value: 'redis://localhost'
        #        - name: POCKETBASE_URL
        #          value: 'http://localhost:8080'
        #        - name: ADMIN_EMAIL
        #          value: 'admin@rize.anonaddy.com'
        #        - name: ADMIN_PASSWORD
        #          valueFrom:
        #            secretKeyRef:
        #              name: admin-password
        #              key: foo
        #
