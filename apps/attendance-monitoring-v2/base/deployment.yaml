apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
  labels:
    app.kubernetes.io/name: attendance-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: attendance-v2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: attendance-v2
    spec:
      volumes:
        - name: pv-storage
          persistentVolumeClaim:
            claimName: pvc
      containers:
      - name: postgres
        image: postgres:17
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: pv-storage
          mountPath: "/var/lib/postgresql/data"
          subPath: "pg_data"
        env:
          - name: TZ
            value: "Asia/Kolkata"
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: laravel
                key: POSTGRES_PASSWORD

      - name: laravel
        image: ghcr.io/navaneeth-dev/attendance-v2:0.0.1@sha256:14900151dfc096791df1e1aac9d04aec63ee935088d657522a26ab5b602c8deb
        ports:
        - containerPort: 8000
        envFrom:
          - configMapRef:
              name: web-config
          - secretRef:
              name: laravel

      - name: worker
        image: ghcr.io/navaneeth-dev/attendance-v2:0.0.1@sha256:14900151dfc096791df1e1aac9d04aec63ee935088d657522a26ab5b602c8deb
        command: ["php"]
        args: ["artisan", "queue:work"]
        envFrom:
          - configMapRef:
              name: web-config
          - secretRef:
              name: laravel

      - name: microservice
        image: ghcr.io/navaneeth-dev/attendance-microservice:main
